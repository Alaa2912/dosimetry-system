# دليل نشر وتشغيل منظومة الرصد الإشعاعي OSL أونلاين على الإنترنت
### Landauer InLight® Dosimetry Cloud Deployment Guide

---

## 🌟 الطرق المتاحة لتشغيل المنظومة أونلاين:

### الخيار الأول (الأفضل والأكثر احترافية): سيرفر سحابي خاص (Cloud VPS)
* **المزودون الموصى بهم:** Hetzner (الأوفر ~4€/شهر)، DigitalOcean (4-6$/شهر)، Contabo، أو Linode.
* **المميزات:** استقرار 24/7، نطاق خاص محمي بشهادة أمان SSL مجانية (HTTPS)، وسرعة استجابة فائقة.

#### خطوات التثبيت على السيرفر (خلال 5 دقائق):
1. قم بإنشاء سيرفر بنظام **Ubuntu 22.04 أو 24.04**.
2. اتصل بالسيرفر عبر Terminal أو PuTTY ونفذ الأوامر التالية:
```bash
# 1. تحديث النظام وتثبيت دوكر
sudo apt update && sudo apt upgrade -y
sudo apt install -y docker.io docker-compose git nginx certbot python3-certbot-nginx

# 2. فك ضغط أو رفع مجلد المنظومة إلى السيرفر
cd /opt
sudo mkdir osl_system && cd osl_system
# (انسخ ملفات المنظومة هنا)

# 3. تشغيل الحاوية عبر Docker Compose
sudo docker-compose up -d --build
```
3. ربط النطاق (Domain) وشهادة الأمان المجانية (HTTPS):
```bash
# تفعيل إعداد Nginx
sudo cp nginx_ssl.conf /etc/nginx/sites-available/osl.conf
sudo ln -s /etc/nginx/sites-available/osl.conf /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx

# استخراج شهادة SSL بضغطة زر واحدة مجاناً
sudo certbot --nginx -d yourdomain.com
```
*أصبح النظام متاحاً رسمياً على الإنترنت عبر الرابط: `https://yourdomain.com`*

---

### الخيار الثاني: نفق كلاودفلير المجاني (Cloudflare Tunnel)
**يعمل مباشرة من كمبيوتر المختبر بدون أي تكلفة وبدون سيرفر خارجي!**
* **المميزات:**
  * مجاني تماماً 100%.
  * لا يحتاج لعنوان IP ثابت ولا فتح بورتات في الراوتر.
  * تبقى قاعدة البيانات والملفات مخزنة بأمان على جهاز المختبر.
* **طريقة التفعيل:**
  1. حمّل برنامج **Cloudflared** على جهاز الويندوز من موقع Cloudflare.
  2. افتح موجه الأوامر (CMD) ونفّذ:
     ```cmd
     cloudflared tunnel --url http://localhost:8000
     ```
  3. سيعطيك رابطاً عالمياً آمناً (مثل: `https://something.trycloudflare.com`) يتيح الدخول للنظام من أي مكان في العالم فوراً!

---

## 👥 نظام صلاحيات وحسابات المستشفيات والمراكز:
* **حساب مدير المنظومة (Admin):**
  * الدخول: `admin` / `admin123` (أو `3la2al7abees@gmail.com`).
  * الصلاحيات: إدارة كافة المستشفيات، استيراد ملفات القارئ InLight، وافتتاح السنوات وإدارة حسابات المنشآت.
* **حسابات المستشفيات (Hospital Users):**
  * يتم إنشاء حساب مخصص لكل منشأة من خلال زر **`👥 حسابات المنشآت`** في الشريط العلوي للمدير.
  * عند دخول مسؤول المستشفى (مثل: `khalidi@hospital.com` / `khalidi123`):
    * تظهر له كوادر وأقسام وتقارير مستشفاه فقط، مع حجب كامل لبيانات المستشفيات الأخرى لضمان السرية والخصوصية الطبية.
